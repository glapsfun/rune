package cli

// nearest returns the candidate closest to token by edit distance and whether a
// close-enough match was found (for did-you-mean suggestions).
func nearest(token string, candidates []string) (string, bool) {
	return "", false // implemented in US3
}
