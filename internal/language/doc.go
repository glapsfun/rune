// Package language provides language intelligence over an analysis Snapshot: the
// symbol index (by name, qualified name, and document), scope lookup, context-aware
// completion, definition resolution, hover content, and the single structured
// registry of Rune built-ins, settings, and attributes (spec FR-026, FR-027). It
// is read-only and never executes anything (FR-028).
package language
